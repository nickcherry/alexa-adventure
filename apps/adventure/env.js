'use strict';

// When building for production, the contents of this file will be replaced
// by `'use strict'; module.exports = 'production';` (see build.sh).
module.exports = process.env.NODE_ENV || 'development';
